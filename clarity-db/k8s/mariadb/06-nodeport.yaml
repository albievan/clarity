apiVersion: v1
kind: Service
metadata:
  name: clarity-mariadb-external
  namespace: clarity-db
  labels:
    app.kubernetes.io/name: clarity-mariadb
    app.kubernetes.io/part-of: clarity
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: clarity-mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      nodePort: 30306   # Fixed port on the Rocky Linux host — change if already in use
